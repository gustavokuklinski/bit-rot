<config>
    <!-- Player Config -->
    <player>
        <speed value="2" />
        <food_water_decay_seconds value="5.0" />
        <food_water_multiplier_decay value="1.5" />
        <food_decay value="0.2" />
        <water_decay value="0.3" />
        <water_autodrink value="true" />
        <water_threshold value="50" />
        <zoom_start value="3.0" />
        <zoom_far value="1.3" />
        <zoom_near value="5.0" />
        <view_radius value="4" />
        <fow_radius value="10" />
    </player>

    <!-- Items Durabilitie Config -->
    <durability>
        <multiplier value="1.0" />
        <weapon_multiplier value="1.0" />
        <tool_multiplier value="1.0" />
    </durability>

    <!-- Spawning Config -->
    <spawning>
        <item_spawn_chance_multiplier value="1.0" />
    </spawning>

    <!-- Zombies Config -->
    <zombie>
        <speed value="1" /> <!-- Zombie Speed -->
        <drop value="1" />  <!-- Zombie Drop -->
        <detection value="20" /> <!-- Zombie detect player radius -->
        <wander value="true" /> <!-- Zombie wander true = wander, false = follow the player -->
        <wander_interval value="2000" /> <!-- wandering interval in MS -->
        <sight_check value="true" />  <!-- Zombie spot the player and go to -->
        <spawn value="3" /> <!-- How many zombies per spawn -->
        <respawn_timer value="30000" /> <!-- Time to Reaspawn Zombie in MS-->
    </zombie>
</config>